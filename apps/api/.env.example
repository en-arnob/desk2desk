# API
PORT=3000
CORS_ORIGIN=http://localhost:5173

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=changeme
DB_NAME=d2d

# Auth
JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRES_IN=7d

# Attachments — base folder for uploaded work files
# (subfolders: <RequesterDept>/<YY>/<MM>/)
UPLOAD_DIR=./uploads
